<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thedraft">

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/newlogo2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserActivity" android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".SignupActivity" android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".JoinClass" android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".MyClasses" android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".ClassLessons" android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".CreateClass" android:theme="@style/AppTheme" >
        </activity>
        <activity android:name=".AddLesson" android:theme="@style/AppTheme" >
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>